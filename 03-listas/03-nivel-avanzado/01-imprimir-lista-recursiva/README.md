# Imprimir una Lista de Forma Recursiva

## Enunciado

Dada una lista, implementa una función que la recorra e imprima sus elementos 
utilizando recursión.

## Ejemplo de ejecución

Insertando los valores `57, 62, 63, 65, 68, 72` al final de la lista (en ese orden), el programa debería imprimir:

```
57 62 63 65 68 72
57 62 63 65 68 72 
```

La primera línea corresponde a la impresión iterativa (ya implementada en la biblioteca), y la segunda a la impresión utilizando la función recursiva a implementar.

## Restricciones
- La solución debe implementarse **únicamente utilizando recursión**.
- El caso base de la recursión corresponde a alcanzar un nodo nulo (final de la lista).

[Volver al nivel básico](..)
